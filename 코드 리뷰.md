# 아리아리 프로젝트 코드 리뷰

> 프로젝트 종료에 앞서 전체 코드베이스를 분석하고, 잘된 부분과 개선이 필요한 부분을 정리한 기술 문서입니다.

---

## 📊 Executive Summary

### 프로젝트 개요
- **Backend**: Spring Boot 3.3.4 + Java 17 (477개 Java 파일)
- **Frontend**: Next.js 14 + React 18 + TypeScript (450+ TypeScript 파일)
- **총 코드 라인**: 약 50,000+ 줄
- **개발 기간**: 2024.01 - 2025.12
- **아키텍처 스타일**: Domain-Driven Design (DDD), Client-Server Architecture

### 종합 평가

| 영역 | 점수 | 평가 |
|------|------|------|
| **Backend 코드 품질** | 8.5/10 | 우수한 아키텍처와 보안 구현, 일부 설정 개선 필요 |
| **Frontend 코드 품질** | 8.5/10 | 탁월한 상태관리와 디자인 시스템, SSR 활용도 개선 필요 |
| **전체 평가** | 8.5/10 | **프로덕션 수준의 잘 설계된 코드베이스** |

### 주요 강점
1. ✅ **탁월한 아키텍처 설계**: DDD 기반의 명확한 계층 분리 (162개 도메인 패키지)
2. ✅ **현대적인 기술 스택**: Spring Boot 3.3.4, Next.js 14 App Router, React Query
3. ✅ **완성도 높은 인증/인가**: JWT + Refresh Token + Redis 블랙리스트
4. ✅ **포괄적인 API 문서화**: Swagger 27개 API 그룹
5. ✅ **타입 안전성**: TypeScript strict 모드 + 포괄적인 타입 정의
6. ✅ **체계적인 에러 처리**: 전역 예외 핸들러 + Sentry 모니터링

### 주요 개선 포인트
1. ⚠️ **테스트 커버리지**: 단위/통합 테스트 부재
2. ⚠️ **SSR 활용**: 대부분의 페이지가 클라이언트 사이드 렌더링
3. ⚠️ **에러 핸들링 일관성**: Frontend에서 에러 처리 패턴 불일치
4. 💡 **CORS 설정**: 하드코딩된 오리진 (외부화 권장)

---

## 🔧 Backend Code Review

### 1. 아키텍처 & 구조 (★★★★★)

#### 1.1 패키지 구조 - Domain-Driven Design

```
com.ariari.ariari/
├── commons/                    # 공통 인프라스트럭처
│   ├── auth/                  # 인증 & OAuth
│   ├── commonentity/          # 기본 엔티티 & 공유 모델
│   ├── entity/                # 핵심 도메인 엔티티
│   ├── exception/             # 전역 예외 처리 (13개 커스텀 예외)
│   ├── manager/               # 비즈니스 매니저 (JWT, Redis, S3, Email)
│   ├── pkgenerator/           # TSID 기반 PK 생성기
│   ├── serializer/            # 커스텀 직렬화
│   └── validator/             # 재사용 가능한 검증기
├── configs/                   # 설정 클래스 (13개 파일)
├── domain/                    # 비즈니스 도메인 (162개 서브패키지)
│   ├── club/                 # 동아리 관리 (16개 서브도메인)
│   │   ├── activity/         # 활동 관리
│   │   ├── alarm/            # 알림
│   │   ├── bookmark/         # 북마크
│   │   ├── club/             # 핵심 동아리 로직
│   │   ├── clubmember/       # 회원 관리
│   │   ├── event/            # 이벤트 & 출석
│   │   ├── faq/              # FAQ
│   │   ├── financial/        # 회계 기록
│   │   ├── notice/           # 공지사항
│   │   ├── question/         # Q&A
│   │   └── review/           # 리뷰
│   ├── member/               # 회원 관리
│   ├── recruitment/          # 모집 & 지원
│   ├── school/               # 학교 인증
│   └── system/               # 시스템 레벨 기능
└── test/                     # 테스트 컨트롤러
```

**✅ 좋은 점:**
- **명확한 관심사의 분리**: 비즈니스 로직이 도메인별로 깔끔하게 조직화
- **재사용 가능한 Commons 패키지**: 중복 코드 최소화
- **깊은 중첩 구조**: 포괄적인 기능 커버리지 반영
- **일관된 패턴**: 각 도메인마다 Controller-Service-Repository 패턴 적용

**참고 파일 경로:**
- `ariari-backend\src\main\java\com\ariari\ariari\domain\club\club\ClubController.java`
- `ariari-backend\src\main\java\com\ariari\ariari\domain\club\club\ClubService.java`
- `ariari-backend\src\main\java\com\ariari\ariari\domain\club\club\ClubRepository.java`

#### 1.2 Controller-Service-Repository 패턴 (★★★★★)

**Controller 계층** (`ClubController.java:30-168`):
```java
@RestController
@RequestMapping("/clubs")
@RequiredArgsConstructor
public class ClubController {
    private final ClubListService clubListService;
    private final ClubService clubService;

    @GetMapping
    public ClubListRes searchClubPage(
        @AuthenticationPrincipal CustomUserDetails userDetails,
        @ModelAttribute ClubSearchCondition condition,
        Pageable pageable) {
        Long reqMemberId = getMemberId(userDetails, false);
        return clubListService.searchClubPage(reqMemberId, condition, pageable);
    }
}
```

**Service 계층** (`ClubService.java:35-157`):
```java
@Service
@Transactional(readOnly = true)  // 기본 읽기 전용
@RequiredArgsConstructor
public class ClubService {
    private final MemberRepository memberRepository;
    private final ClubRepository clubRepository;

    @Transactional  // 쓰기 작업만 명시적으로 표시
    public ClubDetailRes findClubDetail(Long reqMemberId, Long clubId, String clientIp) {
        // 비즈니스 로직
    }
}
```

**✅ 좋은 점:**
- 서비스는 기본적으로 `@Transactional(readOnly = true)` 적용 (성능 최적화)
- 쓰기 작업만 명시적으로 `@Transactional` 표시
- `@RequiredArgsConstructor`를 통한 생성자 주입 (115개 발견)
- 45개 서비스 파일에서 115개의 `@Transactional` 어노테이션 발견

#### 1.3 도메인 모델 설계 (★★★★★)

**기본 엔티티 계층** (`BaseEntity.java`, `LogicalDeleteEntity.java`):
```java
@MappedSuperclass
@Getter
public class BaseEntity {
    @CreationTimestamp
    @Column(updatable = false)
    private LocalDateTime createdDateTime;

    @UpdateTimestamp
    private LocalDateTime updatedDateTime;
}

@MappedSuperclass
public class LogicalDeleteEntity extends BaseEntity {
    private LocalDateTime deletedDateTime;
}
```

**도메인 엔티티 예시** (`Club.java:18-113`):
```java
@Entity
@NoArgsConstructor
@Getter
@SQLDelete(sql = "UPDATE club SET deleted_date_time= CURRENT_TIMESTAMP WHERE club_id= ?")
@SQLRestriction("deleted_date_time is null")
public class Club extends LogicalDeleteEntity implements ViewsContent {
    @Id @CustomPkGenerate
    @Column(name = "club_id")
    private Long id;

    @OneToMany(mappedBy = "club", cascade = CascadeType.REMOVE)
    private List<ClubMember> clubMembers = new ArrayList<>();
    // ... 양방향 관계 설정
}
```

**✅ 좋은 점:**
- **Soft Delete 패턴**: `@SQLDelete`와 `@SQLRestriction`으로 논리적 삭제 구현
- **커스텀 PK 생성기**: TSID 기반 ID 생성 (분산 시스템 친화적)
- **풍부한 도메인 모델**: 포괄적인 관계 설정과 비즈니스 로직
- **불변성**: 민감한 필드에는 `@Setter` 없이 `@Getter`만 사용
- **자동 타임스탬프**: Hibernate 어노테이션으로 생성/수정일 자동 관리

### 2. 코드 품질 (★★★★☆)

#### 2.1 보안 구현 (★★★★☆)

**Spring Security 설정** (`SpringSecurityConfig.java:22-107`):
```java
@Configuration
@EnableWebSecurity
@RequiredArgsConstructor
public class SpringSecurityConfig {
    private final JwtAuthenticationFilter jwtAuthenticationFilter;

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http.csrf(AbstractHttpConfigurer::disable);
        http.sessionManagement(session ->
            session.sessionCreationPolicy(SessionCreationPolicy.STATELESS)
        );
        http.authorizeHttpRequests(authorize -> authorize
            .requestMatchers(PERMIT_ALL_PATTERNS).permitAll()
            .anyRequest().authenticated());
        http.addFilterBefore(jwtAuthenticationFilter,
            UsernamePasswordAuthenticationFilter.class);
        return http.build();
    }
}
```

**JWT 인증 필터** (`JwtAuthenticationFilter.java:28-83`):
```java
@Component
@RequiredArgsConstructor
public class JwtAuthenticationFilter extends OncePerRequestFilter {
    @Override
    protected void doFilterInternal(HttpServletRequest request,
                                    HttpServletResponse response,
                                    FilterChain filterChain) {
        String ip = request.getHeader("X-Forwarded-For");
        String uri = request.getRequestURI();
        boolean isSuspicious = isSuspiciousUri(uri);
        securityLogService.SaveSecurityAccessLog(ip, uri, isSuspicious);

        String token = jwtManager.extractToken(request);
        if (token != null) {
            JwtAuthentication jwtAuthentication = new JwtAuthentication(token);
            Authentication authenticate = authenticationManager.authenticate(jwtAuthentication);
            SecurityContextHolder.getContext().setAuthentication(authenticate);
        }
        filterChain.doFilter(request, response);
    }
}
```

**JWT 토큰 블랙리스트** (`JwtControlManager.java:14-41`):
```java
@Component
@RequiredArgsConstructor
public class JwtControlManager {
    private final RedisManager redisManager;

    public boolean isBannedToken(String token) {
        Object data = redisManager.getData(resolveRedisKey(token));
        return data != null;
    }

    public void banToken(String token, Date date) {
        long diffMilli = date.getTime() - now.getTime();
        redisManager.setExData(resolveRedisKey(token), true,
                              diffMilli, TimeUnit.MILLISECONDS);
    }
}
```

**✅ 좋은 점:**
- JWT 기반 무상태 인증 (JJWT 0.12.3)
- Redis를 활용한 토큰 블랙리스트 (로그아웃 기능)
- 커스텀 예외를 통한 체계적인 검증
- 의심스러운 URI 접근 로깅
- Access/Refresh Token 분리
- BCrypt 비밀번호 암호화

**⚠️ 개선 필요:**
- `PERMIT_ALL_PATTERNS`가 과도하게 허용적 (`SpringSecurityConfig.java:29-83`)
- `JwtAuthenticationFilter.java:77`에 CORS 오리진 하드코딩
- 설정 프로퍼티로 외부화 필요

#### 2.2 예외 처리 (★★★★★)

**전역 예외 핸들러** (`ExceptionControllerAdvice.java:20-98`):
```java
@RestControllerAdvice
public class ExceptionControllerAdvice {
    @ResponseStatus(HttpStatus.INTERNAL_SERVER_ERROR)
    @ExceptionHandler
    public ExceptionRes handleInternalException(HttpServletRequest request, Exception e) {
        log.error("exception !!", e);
        Sentry.captureException(e);  // Sentry 연동!
        return ExceptionRes.builder()
            .code(HttpStatus.INTERNAL_SERVER_ERROR.value())
            .message(e.getMessage())
            .build();
    }

    @ResponseStatus(HttpStatus.BAD_REQUEST)
    @ExceptionHandler
    public ExceptionRes handleCustomException(HttpServletRequest request, CustomException e) {
        log.info("exception", e);
        Sentry.captureException(e);
        return ExceptionRes.builder()
            .code(e.getHttpStatus().value())
            .message(e.getMessage())
            .build();
    }
}
```

**✅ 좋은 점:**
- `commons/exception/exceptions/`에 13개 이상의 커스텀 예외
- `@RestControllerAdvice`를 통한 중앙화된 에러 처리
- **Sentry 연동**으로 에러 추적 (`build.gradle:86`)
- 일관된 에러 응답 포맷
- HTTP 상태 코드 매핑
- `@Valid` 어노테이션에 대한 검증 예외 처리

#### 2.3 QueryDSL 활용 (★★★★★)

**커스텀 리포지토리 구현** (`ClubRepositoryImpl.java:31-251`):
```java
@RequiredArgsConstructor
public class ClubRepositoryImpl implements ClubRepositoryCustom {
    private final JPAQueryFactory queryFactory;

    @Override
    public Page<ClubData> searchClubPage(School schoolParam,
                                         ClubSearchCondition condition,
                                         Pageable pageable) {
        JPAQuery<ClubData> query = queryFactory
            .select(ClubData.projection())
            .from(club)
            .leftJoin(club.school, school)
            .where(schoolIsNull().or(schoolEq(schoolParam)),
                   categoryIn(condition.getClubCategoryTypes()),
                   regionIn(condition.getClubRegionTypes()),
                   participantIn(condition.getParticipantTypes()))
            .offset(pageable.getOffset())
            .limit(pageable.getPageSize());

        // Pageable에서 동적 정렬 적용
        for (Sort.Order o : pageable.getSort()) {
            PathBuilder pathBuilder = new PathBuilder(club.getType(), club.getMetadata());
            query.orderBy(new OrderSpecifier<>(
                o.isAscending() ? Order.ASC : Order.DESC,
                pathBuilder.get(o.getProperty())));
        }

        List<ClubData> content = query.fetch();
        Long total = queryFactory.select(club.count()).from(club)...;

        return new PageImpl<>(content, pageable, total);
    }

    private BooleanExpression categoryIn(List<ClubCategoryType> categoryTypes) {
        return categoryTypes.isEmpty() ? null : club.clubCategoryType.in(categoryTypes);
    }
}
```

**✅ 좋은 점:**
- 컴파일 타임 타입 체크가 가능한 타입 안전한 쿼리
- BooleanExpression으로 동적 필터링
- 페이지네이션 지원
- Spring Data Pageable에서 동적 정렬
- Custom Repository 패턴으로 깔끔한 분리
- 다수의 커스텀 리포지토리 구현 발견

#### 2.4 MyBatis 전략적 활용 (★★★★☆)

**MyBatis 매퍼** (`ClubActivityMapper.java:9-31`):
```java
@Mapper
public interface ClubActivityMapper {
    List<ClubActivityData> findClubActivityForClubMember(
        Long clubId, Long memberId, int size, int offset);

    int findClubActivityForClubMemberCount(Long clubId, int size, int offset);
}
```

**XML 매핑** (`ClubActivity.xml:63-83`):
```xml
<select id="findClubActivityForClubMember">
    SELECT ca.club_activity_id AS id,
           ca.created_date_time,
           ca.member_id AS creatorId,
           cm2.name AS creatorName,
           (SELECT COUNT(*) FROM club_activity_like cal
            WHERE cal.club_activity_id = ca.club_activity_id) AS likeCount,
           CASE WHEN cal.club_activity_like_id IS NULL THEN FALSE
                ELSE TRUE END AS isMyLiked
    FROM club_activity ca
    LEFT JOIN club_activity_like cal ON cal.club_activity_id = ca.club_activity_id
                                     AND cal.member_id = #{memberId}
    WHERE ca.deleted_date_time IS NULL AND ca.club_id = #{clubId}
    ORDER BY ca.created_date_time DESC
    LIMIT #{size} OFFSET #{offset}
</select>
```

**✅ 좋은 점:**
- **복잡한 쿼리에만 사용**: 다중 서브쿼리가 필요한 경우
- 단순 쿼리는 JPA/QueryDSL 사용
- 명확한 관심사 분리
- 2개의 MyBatis 매퍼만 발견 (PassReview, ClubActivity)

**💡 관찰**: MyBatis는 복잡한 집계가 필요한 성능 중요 쿼리에만 전략적으로 사용하고, 표준 CRUD는 JPA가 처리

### 3. 혁신적인 구현 (★★★★★)

#### 3.1 TSID 기반 커스텀 PK 생성기

**CustomPkGenerator.java:17-24**:
```java
public class CustomPkGenerator implements IdentifierGenerator {
    @Override
    public Object generate(SharedSessionContractImplementor session, Object o) {
        return TsidCreator.getTsid().toLong();  // Time-Sorted ID
    }
}
```

**장점:**
- UUID 대신 **TSID**(Time-Sorted Unique Identifier) 사용
- 데이터베이스 인덱싱과 정렬에 유리
- 분산 시스템 친화적

#### 3.2 Soft Delete 구현

```java
@SQLDelete(sql = "UPDATE club SET deleted_date_time= CURRENT_TIMESTAMP WHERE club_id= ?")
@SQLRestriction("deleted_date_time is null")
public class Club extends LogicalDeleteEntity {
    // repository.delete() 시 자동으로 soft delete 실행
}
```

**장점:**
- Hibernate가 관리하는 소프트 삭제
- 비즈니스 로직에 투명
- 감사(Audit) 친화적

#### 3.3 포괄적인 Swagger 문서화

**SwaggerConfig.java에 27개 API 그룹 설정**:
```java
@Bean
public GroupedOpenApi all() {
    return GroupedOpenApi.builder()
        .group("02. 전체 API")
        .packagesToScan("com.ariari.ariari")
        .build();
}
// ... 인증, 회원, 동아리, 모집 등 26개 그룹 추가
```

#### 3.4 보안 로깅 & 모니터링

```java
String ip = request.getHeader("X-Forwarded-For");
String uri = request.getRequestURI();
boolean isSuspicious = isSuspiciousUri(uri);
securityLogService.SaveSecurityAccessLog(ip, uri, isSuspicious);
```

**장점:**
- 사전 보안 모니터링
- 의심스러운 URI 탐지
- Sentry 연동으로 에러 추적

### 4. Backend 개선 포인트

#### 🔴 Critical (즉시 수정 필요)

**1. Frontend 보안 취약점** (`app\api\api.ts`)

**문제**: 소스 코드에 하드코딩된 authorization 토큰

**조치**: 즉시 제거, 환경 변수 사용

#### 🟡 Moderate (중요도 중간)

**2. CORS 설정 하드코딩** (`CorsConfig.java:18-21`)

```java
.allowedOrigins("http://localhost:3000",
                "https://main.d2ux83pqp6klo1.amplifyapp.com/",
                "https://www.ariari.kr",
                "http://localhost:3001")  // 하드코딩 + 개발 포트 노출
.allowedHeaders("*")  // 너무 허용적
```

**권장사항**:
```java
@Value("${cors.allowed-origins}")
private String[] allowedOrigins;

registry.addMapping("/**")
    .allowedOrigins(allowedOrigins)
    .allowedMethods("GET", "POST", "PUT", "DELETE", "PATCH")
    .allowedHeaders("Authorization", "Content-Type")
    .allowCredentials(true);
```

**3. Spring Security PERMIT_ALL 패턴** (`SpringSecurityConfig.java:29-83`)

```java
private static final String[] PERMIT_ALL_PATTERNS = {
    "/clubs/**",
    "/recruitments/**",
    "/schools/**",
    "/members/**",
    // ... 25개 이상의 패턴
};
```

**설계 의도**: 아리아리는 **비회원도 동아리/모집 정보를 조회**할 수 있는 기획으로, 많은 엔드포인트가 공개 접근 가능하도록 의도적으로 설계됨

**✅ 현재 구현이 적절한 이유**:
- 동아리 목록/상세 조회는 비회원에게도 공개
- 모집공고 검색/조회 기능도 공개
- 회원가입 전 서비스 탐색 유도 (전환율 향상 전략)

**💡 개선 제안** (선택사항):
- 읽기 작업과 쓰기 작업을 명확히 분리
- 쓰기 작업에만 메서드 레벨 보안 적용
```java
@PreAuthorize("isAuthenticated()")
@PostMapping
public void createClub(...) { }
```

**4. 예외 메시지 노출**

```java
.message(e.getMessage()) // 배포 후 수정 예정 (주석에 명시)
```

**권장사항**:
- 프로덕션에서 내부 예외 메시지 노출 금지
- 일반적인 메시지 사용
- 상세 에러는 서버 로그만

**5. 설정 파일 관리**

**현황**: `application.yml`이 레포지토리에 없음

**실제 상황**:
- 개발/프로덕션 환경별 `application.yml` 파일은 존재
- **보안상 이유로 Git에서 제외**, 팀 내부에서 별도 공유
- DB 접속 정보, API 키 등 민감한 정보 포함

**✅ 베스트 프랙티스 준수**:
- `.gitignore`에 `application*.yml` 추가
- 템플릿 파일(`application-template.yml`) 제공 권장
- 필수 환경 변수 README에 문서화

**6. API 버전 관리 없음**

**권장사항**:
```java
@RequestMapping("/api/v1/clubs")
```

**7. 테스트 커버리지 부족**

**현황**: 테스트 컨트롤러만 발견 (S3TestController, AuthTestController)

**권장사항**:
- 서비스 단위 테스트 추가
- 리포지토리 통합 테스트
- 인증/인가 보안 테스트

---

## 🎨 Frontend Code Review

### 1. 아키텍처 & 구조 (★★★★★)

#### 1.1 Next.js 14 App Router 활용

**디렉토리 구조 (450+ TypeScript 파일)**:
```
app/
├── (home)/            # 홈 관련 라우트 그룹
├── (club)/            # 동아리 관련 라우트 그룹
├── (auth)/            # 인증 관련 라우트 그룹
├── (recruitment)/     # 모집 관련 라우트 그룹
├── components/        # 재사용 가능한 UI 컴포넌트
│   ├── alert/
│   ├── badge/
│   ├── button/       # 세분화 (basicBtn, iconBtn, withIconBtn)
│   ├── card/
│   ├── modal/
│   └── ...
├── api/              # API 레이어
├── hooks/            # 커스텀 훅
├── stores/           # Zustand 스토어
├── types/            # TypeScript 타입
├── constants/        # 상수
└── context/          # React Context
```

**✅ 좋은 점:**
- **Route Groups 활용**: 논리적 구조화 (URL에 영향 없음)
- **중첩 레이아웃**: `(club)/club/layout.tsx`에서 ClubProvider로 범위 지정된 상태 관리
- **동적 임포트**: `dynamic()`으로 코드 스플리팅
- **Suspense 경계**: Loading 컴포넌트

**⚠️ 개선 필요:**
- 많은 페이지가 `"use client"` + `ssr: false` 사용 (SSR 이점 상실)
- Server Components를 더 활용할 수 있음
- 모달에 Parallel Routes나 Intercepting Routes 미사용

#### 1.2 Zustand를 활용한 상태 관리 (★★★★★)

**AuthStore 구현** (`app\stores\authStore.ts`):
```typescript
export const useAuthStore = create<AuthState>()(
  persist(
    (set) => ({
      accessToken: null,
      refreshToken: null,
      oauthSignUpKey: null,
      setAuth: ({ accessToken, refreshToken, oauthSignUpKey }) =>
        set({ accessToken, refreshToken, oauthSignUpKey }),
      logout: () =>
        set({ accessToken: null, refreshToken: null, oauthSignUpKey: null }),
    }),
    {
      name: "ariari-auth",
      storage: createJSONStorage(() =>
        typeof window !== "undefined" ? localStorage : undefined!
      ),
      partialize: (state) => ({...}),
    }
  )
);
```

**✅ 좋은 점:**
- 깔끔하고 최소한의 상태 관리 (2개 스토어만: auth, user)
- `persist` 미들웨어로 localStorage 활용
- SSR 안전: `typeof window !== "undefined"` 체크
- userStore에서 `hasHydrated` 플래그로 하이드레이션 추적
- TypeScript 인터페이스로 타입 안전

**베스트 프랙티스:**
- `partialize`로 영속화 대상 제어
- `onRehydrateStorage` 콜백으로 하이드레이션 추적
- 명확한 액션 네이밍 (setUser, clearUser, setAuth, logout)

#### 1.3 API 통합 패턴 (★★★★☆)

**토큰 갱신 구현** (`app\api\axiosInstance.ts`):
```typescript
// 정교한 토큰 갱신 + 요청 큐잉
let isRefreshing = false;
let failedQueue: Array<{...}> = [];

axiosInstance.interceptors.response.use(
  (response) => response,
  async (error) => {
    // 갱신 중 실패한 요청을 큐에 저장
    if (isRefreshing) {
      return new Promise((resolve, reject) => {
        failedQueue.push({...});
      });
    }
    // 401 시 토큰 갱신 시도
    // 갱신 후 큐에 저장된 요청 재실행
  }
);
```

**✅ 좋은 점:**
- 탁월한 토큰 갱신 로직 + 요청 큐잉
- 다중 동시 갱신 방지
- 토큰 갱신 후 실패한 요청 자동 재시도
- 갱신 실패 시 사용자 알림 후 로그아웃

**⚠️ 문제점:**
- `app\api\api.ts`에 하드코딩된 authorization 토큰 (보안 위험!)
- API 모듈 간 에러 처리 불일치 (일부는 try/catch, 일부는 안함)

### 2. 코드 품질 (★★★★☆)

#### 2.1 TypeScript 사용 및 타입 안전성 (★★★★☆)

**타입 정의 예시** (`app\types\api.ts`):
```typescript
export type ClubData = {
  id: string;
  name: string;
  profileUri: string;
  // ... 잘 정의된 타입들
}

export type ClubResponse = {
  clubDataList: ClubData[];
  pageInfo: PageInfo;
};
```

**✅ 좋은 점:**
- Strict 모드 활성화: `tsconfig.json`에 `"strict": true`
- `app/types/` 디렉토리에 포괄적인 타입 정의
- 도메인별 타입 조직화 (api.ts, club.ts, recruitment.ts 등)
- 타입 안전한 API 응답

**⚠️ 개선 필요:**
- `noUnusedLocals: false` - strict 체크를 위해 활성화 필요
- 일부 컴포넌트 props에 `any` 타입 사용
- 선택적 파라미터 더 엄격하게 타입 지정

#### 2.2 React Query 활용 (★★★★★)

**Query Client 설정**:
```typescript
new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 1000 * 60,     // 1분
      retry: false,              // 자동 재시도 없음
      refetchOnWindowFocus: false,
    },
  },
})
```

**커스텀 훅 패턴**:
```typescript
// Query Hook
export const useRecruitmentDetailQuery = (recruitmentId: string) => {
  return useQuery({
    queryKey: QUERY_KEYS.recruitmentDetail(recruitmentId),
    queryFn: () => getRecruitmentDetail(recruitmentId),
    select: (data) => data,
  });
};

// Mutation Hook with 캐시 무효화
export const useSubmitApplicationMutation = ({
  recruitmentId, onSuccess, onError
}: ApplyMutationProps) => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: ({recruitmentId, formData}) =>
      postApplication(recruitmentId, formData),
    onSuccess: () => {
      onSuccess();
      queryClient.invalidateQueries({
        queryKey: [QUERY_KEYS.myApplyList]
      });
      queryClient.invalidateQueries({
        queryKey: QUERY_KEYS.recruitmentDetail(recruitmentId),
      });
    },
    onError: () => { onError(); },
  });
};
```

**✅ 좋은 점:**
- 중앙화된 쿼리 키: `app\constants\queryKeys.ts`
- Mutation 시 적절한 캐시 무효화
- 커스텀 훅으로 깔끔한 추상화
- 개발용 DevTools 활성화
- 타입 안전한 query/mutation 훅

**베스트 프랙티스:**
- 파라미터화된 쿼리 키를 위한 팩토리 함수
- 일관된 훅 네이밍 컨벤션
- 콜백 기반 성공/에러 핸들링

### 3. UI/UX 구현 (★★★★★)

#### 3.1 Tailwind CSS 디자인 시스템

**설정** (`tailwind.config.ts`):
```typescript
theme: {
  extend: {
    colors: {
      primary: { ... },
      text: { gray1, gray2, gray3, ... },
      token: { purple, blue, ... },
    },
    fontSize: {
      h1_contents_title: ['28px', { lineHeight: '140%', fontWeight: '700' }],
      mobile_h3: ['18px', { lineHeight: '140%', fontWeight: '700' }],
      // ... 모바일/데스크톱 변형
    },
    // 커스텀 스크롤바, 그림자, border-radius 등
  }
}
```

**✅ 좋은 점:**
- 일관된 디자인 토큰 (색상, 간격, 타이포그래피)
- 잘 정의된 컬러 팔레트
- 커스텀 유틸리티 (그림자, border-radius)
- 가중치와 행간이 포함된 폰트 시스템
- 반응형 브레이크포인트: sm, md, lg, lx

**사용 패턴 예시**:
```typescript
className="w-full p-4 md:px-6 md:py-[26px] rounded-lg bg-background
  focus:bg-hover md:hover:bg-hover md:focus:bg-pressed cursor-pointer"
```

- 반응형 수정자 적절히 사용
- 인터랙티브 상태 (hover, focus, pressed)
- 의미론적 색상 이름

#### 3.2 반응형 디자인 (★★★★☆)

**커스텀 훅** (`app\hooks\useResponsive.ts`):
```typescript
const useResponsive = (screen: "sm" | "md" | "lg" | "lx"): boolean => {
  const [isBreakPoint, setIsBreakPoint] = useState<boolean>(false);

  useEffect(() => {
    const mediaQueryList = window.matchMedia(query);
    const updateBreakPoint = () => setIsBreakPoint(mediaQueryList.matches);

    updateBreakPoint();
    mediaQueryList.addEventListener("change", updateBreakPoint);

    return () => mediaQueryList.removeEventListener("change", updateBreakPoint);
  }, [query]);

  return isBreakPoint;
};
```

**사용 예시**:
```typescript
const isMd = useResponsive("md");
{showProfileModal && (isMd ?
  <ProfileSettingModal /> :
  <MobileProfileSettingModal />
)}
```

**✅ 좋은 점:**
- 모바일 우선 접근
- 브레이크포인트 감지를 위한 `useResponsive` 훅
- 조건부 컴포넌트 렌더링
- 별도의 모바일/데스크톱 타이포그래피 스케일

**⚠️ 개선 필요:**
- `useResponsive` 훅 의존으로 클라이언트 사이드 렌더링 강제
- SSR 호환성을 위해 CSS 미디어 쿼리를 더 활용 가능
- 모바일/데스크톱 중복 컴포넌트 (유지보수 오버헤드)

#### 3.3 컴포넌트 재사용성 (★★★★★)

**NotiPopUp 예시**:
```typescript
<NotiPopUp
  icon={"check"}
  title={"모집을 종료할까요?"}
  description={"..."}
  modalType={"button"}
  firstButton={handleEnd}
  firstButtonText={"모집 종료하기"}
  secondButton={close}
  secondButtonText={"취소하기"}
/>
```

**✅ 좋은 점:**
- 풍부한 버튼 라이브러리 (basicBtn, iconBtn, withIconBtn 변형)
- 재사용 가능한 카드 컴포넌트
- 범용 모달/팝업 컴포넌트
- 공유 인풋 컴포넌트
- Discriminated unions으로 모달 타입 구분
- 명확한 props로 조합 가능한 컴포넌트

### 4. Frontend 개선 포인트

#### 🔴 Critical (즉시 수정 필요)

**1. 보안 취약점** (`app\api\api.ts`)

**문제**: 소스 코드에 하드코딩된 authorization 토큰

**조치**: 즉시 제거, 환경 변수 사용

**2. 에러 핸들링 불일치**

```typescript
// 좋음 - 구조화된 에러 핸들링
export const getRecruitmentDetail = async (recruitmentId: string) => {
  try {
    const response = await axiosInstance.get<RecruitmentResponse>(...);
    return response.data;
  } catch (err) {
    if (err instanceof AxiosError) {
      if (err.response && err.response.data.message) {
        throw new Error(err.response.data.message);
      }
    }
    throw new Error("모집공고를 찾을 수 없습니다.");
  }
};

// 나쁨 - console.error만
export const getInternalRecruitmentRanking = async () => {
  try {
    const { data } = await axiosInstance.get(...);
    return data;
  } catch (err) {
    console.error("조회 실패:", err);
  }
};
```

**문제점:**
- 전역 에러 바운더리 없음
- 일부 API 함수가 에러를 삼킴 (에러 시 undefined 반환)
- 에러 핸들링 패턴 불일치
- 사용자 대면 에러가 `alert()` 사용

**권장사항:**
- 전역 에러 바운더리 추가
- 에러 핸들링 패턴 표준화
- Toast 알림 시스템 구현

#### 🟡 Moderate (중요도 중간)

**3. 클라이언트 컴포넌트 과다 의존**

**문제**:
- 대부분 페이지가 `"use client"` 사용
- 많은 컴포넌트가 `ssr: false` 사용
- Server Components 이점 상실

**예시**:
```typescript
"use client";
import dynamic from "next/dynamic";

const HomePageContent = dynamic(() => import("@/(home)/home/pageContent"), {
  loading: () => <Loading />,
  ssr: false,  // ❌ SSR 이점 상실
});
```

**권장사항:**
- 정적 콘텐츠는 Server Components 사용
- 초기 렌더는 SSR, 이후 인터랙티브로 하이드레이션
- Suspense로 스트리밍 SSR 활용

**4. TypeScript 엄격성**

**현재**: `noUnusedLocals: false`

**권장**: 엄격한 체크 활성화, `any` 타입 제거

**5. Next.js 설정**

```javascript
reactStrictMode: false,  // 개발 시 true로 설정 필요
```

**6. 테스트 인프라**

**현황**: 테스트 파일 없음

**권장사항**:
- Jest/Vitest 추가
- React Testing Library 구현
- 단위/통합 테스트

**7. 애니메이션 활용도**

**현황**: Framer Motion 설치되었으나 1개 파일에서만 사용

**권장**:
- 로딩 트랜지션 추가
- 페이지 전환 효과
- 인터랙션 피드백

---

## 📈 종합 평가 및 권장사항

### 전체 강점 요약

| 영역 | 평가 | 주요 강점 |
|------|------|-----------|
| **아키텍처** | ⭐⭐⭐⭐⭐ | DDD 설계, Route Groups, 계층 분리 |
| **보안** | ⭐⭐⭐⭐ | JWT + Refresh Token, Redis 블랙리스트, Sentry |
| **상태 관리** | ⭐⭐⭐⭐⭐ | Zustand 미니멀리즘, React Query 통합 |
| **데이터 접근** | ⭐⭐⭐⭐⭐ | QueryDSL + MyBatis + JPA 하이브리드 |
| **디자인 시스템** | ⭐⭐⭐⭐⭐ | 포괄적인 Tailwind 설정, 일관된 토큰 |
| **코드 품질** | ⭐⭐⭐⭐ | Lombok, 검증, 예외 처리, Soft Delete |
| **API 문서화** | ⭐⭐⭐⭐⭐ | 27개 Swagger 그룹 |
| **타입 안전성** | ⭐⭐⭐⭐ | TypeScript Strict, 포괄적 타입 정의 |

### 우선순위별 개선 사항

#### 🔴 High Priority (즉시 조치)

1. **Frontend 보안 취약점 제거**
   - `app\api\api.ts`의 하드코딩된 토큰 제거
   - 환경 변수로 마이그레이션

2. **전역 에러 처리 표준화**
   - Backend: 예외 메시지 노출 제한
   - Frontend: 전역 에러 바운더리 추가
   - Toast 알림 시스템 구현

3. **테스트 인프라 구축**
   - Backend: JUnit 5, MockMvc, TestContainers
   - Frontend: Jest/Vitest, React Testing Library
   - 단위 및 통합 테스트

#### 🟡 Medium Priority (중요)

4. **CORS 설정 외부화**
   - 하드코딩된 오리진을 환경 변수로 이동
   - `@ConfigurationProperties` 사용

5. **SSR 활용도 개선**
   - `ssr: false` 제거
   - Server Components 활용 증가
   - 스트리밍 SSR with Suspense

6. **API 버전 관리**
   - `/api/v1/` prefix 추가
   - 버전 전략 수립

7. **에러 핸들링 일관성**
   - API 함수 에러 처리 표준화
   - Result 타입 또는 일관된 throw 패턴

#### 🟢 Low Priority (개선)

8. **애니메이션 향상**
   - Framer Motion 활용
   - 로딩/페이지 전환 효과
   - 마이크로 인터랙션

9. **TypeScript 엄격성**
    - `noUnusedLocals: true`
    - `any` 타입 제거
    - 더 엄격한 타입 체크

10. **반응형 패턴 통합**
    - CSS 미디어 쿼리 더 활용
    - 모바일/데스크톱 중복 컴포넌트 통합

11. **React Strict Mode**
    - 개발 환경에서 활성화
    - 잠재적 문제 조기 발견

### 학습 포인트 및 베스트 프랙티스

#### ✅ 다음 프로젝트에서도 유지할 것

1. **TSID 기반 PK 생성**: 분산 시스템 친화적
2. **Soft Delete 패턴**: 데이터 복구 가능성
3. **QueryDSL + JPA 조합**: 타입 안전 + 유연성
4. **React Query 통합**: 서버 상태 관리 단순화
5. **Tailwind 디자인 시스템**: 일관성 + 생산성
6. **토큰 갱신 큐잉**: 견고한 인증 플로우
7. **DDD 아키텍처**: 확장 가능한 구조
8. **Zustand 미니멀리즘**: 상태 관리 단순화

#### 🔄 개선하여 적용할 것

1. **테스트 주도 개발**: 초기부터 테스트 작성
2. **SSR 우선 접근**: 필요한 곳만 클라이언트 컴포넌트
3. **설정 관리**: `.gitignore` + 템플릿 파일 제공
4. **API 버전 관리**: 초기 설계부터 고려
5. **에러 핸들링 전략**: 프로젝트 시작 시 정의
6. **문서화**: JSDoc + README + API 문서

### 최종 평가

**아리아리 프로젝트는 프로덕션 수준의 코드 품질을 보여주는 잘 설계된 풀스택 애플리케이션입니다.**

**핵심 성과:**
- 🏗️ 견고한 아키텍처 설계 (DDD + Clean Architecture)
- 🔐 완성도 높은 보안 구현 (JWT + Redis + Sentry)
- 🎨 체계적인 디자인 시스템 (Tailwind + 컴포넌트 라이브러리)
- 📊 효율적인 데이터 관리 (QueryDSL + MyBatis + React Query)
- 📱 반응형 UI/UX (모바일/데스크톱 최적화)

**개선 영역:**
- 🧪 테스트 커버리지 확대
- ⚡ SSR 활용도 증대
- 🔧 CORS 설정 외부화
- 🎨 에러 핸들링 일관성

**종합 점수: 8.5/10**

이 프로젝트를 통해 얻은 기술적 노하우와 아키텍처 설계 경험은 차기 프로젝트의 탄탄한 기반이 될 것입니다.

---

*작성일: 2025.12*
*분석 대상: ariari-backend (477 Java files), ariari-frontend (450+ TypeScript files)*
