export interface ListSectionProps {
  date: string;
  title: string;
  point: number;
}

export interface DayFloatingBarProps {
  deductionPoint: number;
  currentPoint: number;
  className?: string;
}
